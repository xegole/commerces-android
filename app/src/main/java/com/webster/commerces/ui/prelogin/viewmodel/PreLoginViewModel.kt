package com.webster.commerces.ui.prelogin.viewmodel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class PreLoginViewModel(application: Application) : AndroidViewModel(application)